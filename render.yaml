services:
  - type: web
    name: chatbot-flask # או השם שבחרת
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app # <- ודא שזה בדיוק מה שכתוב כאן
    envVars:
      - key: OPENAI_API_KEY
        sync: false
